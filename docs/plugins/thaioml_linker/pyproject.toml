[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "thaioml-linker"
version = "0.1.0"
dependencies = ["mkdocs>=1.5.0", "pyyaml"]

[project.entry-points."mkdocs.plugins"]
thaioml-linker = "thaioml_linker.plugin:ThaiOMLLinkerPlugin"
